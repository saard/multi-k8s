name: tests
on: push
jobs:
  run-test:
    runs-on: ubuntu-latest
    steps:
      - name: prepare
        run: docker build ../client/  -f ../client/Dockerfile.dev -t saardagan/react-test
      - name: execute
        run: docker run saardagan/react-test npm test -- --coverage